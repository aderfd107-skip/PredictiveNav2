# 04：定义 Track 轨迹数据

目的：在 C++ 中定义一条轨迹长期需要保存的内容，而不是每帧临时使用一个 cluster。

第一版 `Track` 会逐步拥有：`track_id`、位置/速度状态、协方差、尺寸、首次与最近观测时间、命中次数 `age`、连续未命中次数 `missed_frames`。

现在先理解“struct 保存一条轨迹状态”的意义；每一帧的 cluster 是输入，Track 是跨回调持续留在 node 成员变量中的对象。
